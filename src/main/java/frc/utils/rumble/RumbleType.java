package frc.utils.rumble;

public enum RumbleType{
    /** 
     * replaces all rumble data
     */
    OVERRIDE,
    /**
     * adds rumble to que
     */
    ADD,
    /**
     * plays rumble over others
     */
    OVERLAY
}